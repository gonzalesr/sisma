@model IEnumerable<SismaV02.Models.Barrio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomBarrio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomBarrio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodBarrio }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodBarrio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodBarrio })
        </td>
    </tr>
}

</table>



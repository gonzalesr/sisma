@model IEnumerable<SismaV02.Models.Privilegio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesPriv)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesPriv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodPrivil }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodPrivil }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodPrivil })
        </td>
    </tr>
}

</table>

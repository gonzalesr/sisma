@model IEnumerable<SismaV02.Models.CatServicio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Nuevo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomCategoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomCategoria)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.CodCategoria }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.CodCategoria }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.CodCategoria })
        </td>
    </tr>
}

</table>

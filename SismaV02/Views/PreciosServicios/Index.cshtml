@model IEnumerable<SismaV02.Models.PrecioServicio>
@section AddToHead{
    <link href="@Url.Content("~/Content/AdminLTE/plugins/datatables/dataTables.bootstrap.css")" rel="stylesheet" type="text/css" />
    <link href="~/Content/AdminLTE/css/skins/_all-skins.min.css" rel="stylesheet" />
}
@{
    ViewBag.Title = "Precios";
}

<h2>Precios</h2>

<p>
    @Html.ActionLink("Nuevo", "Create")
</p>
<div class="box">
    <div class="box-header">
        <h3 class="box-title"> </h3>
    </div>

    <div class="box-body">
        <table id="listaModelo" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PrecioUnit)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CatServicio.NomCategoria)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Servicio.Descripcion)
                    </th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.PrecioUnit)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CatServicio.NomCategoria)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Servicio.Descripcion)
                        </td>
                        <td>
                            @*@Html.ActionLink("Editar", "Edit", new { CodCategoria = item.CodCategoria, CodServicio = item.CodServicio }) |
                            @Html.ActionLink("Detalles", "Details", new { /* id=item.PrimaryKey */ }) |
                            @Html.ActionLink("Eliminar", "Delete", new { /* id=item.PrimaryKey */ })*@
                            <a class="btn btn-default btn-sm" href="@Url.Action("Edit", "PreciosServicios", new { CodCategoria = item.CodCategoria, CodServicio = item.CodServicio })" ><span class='glyphicon glyphicon-pencil'></span></a>
                            <a class="btn btn-info btn-sm" href="@Url.Action("Details", "PreciosServicios", new { CodCategoria = item.CodCategoria, CodServicio = item.CodServicio })"><span class='glyphicon glyphicon-tasks'></span></a>
                            <a class="btn btn-danger btn-sm"href="@Url.Action("Delete", "PreciosServicios", new { CodCategoria = item.CodCategoria, CodServicio = item.CodServicio })"><span class='glyphicon glyphicon-trash'></span></a>
                        </td>
                    </tr>
                }

            </tbody>

            <tfoot>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.PrecioUnit)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CatServicio.NomCategoria)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Servicio.Descripcion)
                    </th>
                    <th></th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/DataTable")
}

